/**
 * ------------------------------------------------------------
 * RatAttacK — Auth Provider Selector
 * ------------------------------------------------------------
 * Single source of truth for the active AuthProvider implementation.
 * When Shopify Customer Accounts are ready, replace the export below
 * with the Shopify provider. UI/routes/context stay unchanged.
 */

export * from './mock-provider';
